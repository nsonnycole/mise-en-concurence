// Generated by CoffeeScript PHP 1.3.1
(function() {
  var selectToggle;

  $(function() {
    $(".js_input_toggle").each(function(num, element) {
      var target, _i, _len, _ref, _results;
      _ref = $(element).data("target-input").split(" ");
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        target = _ref[_i];
        _results.push(selectToggle($(element), $("." + target)));
      }
      return _results;
    });
    return $("form").delegate(".js_input_toggle", "change", function(event) {
      var element, _i, _len, _ref, _results;
      _ref = $(event.currentTarget).data("target-input").split(" ");
      _results = [];
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        element = _ref[_i];
        _results.push(selectToggle($(event.currentTarget), $("." + element)));
      }
      return _results;
    });
  });


  selectToggle = function(trigger, input) {
    var value;
    if (trigger.is("select")) {
      value = trigger.val();
    } else {
      value = $(trigger).find("input:checked").val();
    }
    if (!value || value === "" || value === "Dès que possible" || value === "no") {
      input.closest(".control-group").hide();
      return input.closest(".control-group input").removeAttr("required");
    } else {
      input.closest(".control-group").show();
      return input.closest(".control-group input").attr("required", "required");
    }
  };

}).call(this);
